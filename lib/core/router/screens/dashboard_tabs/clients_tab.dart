import 'package:flutter/material.dart';

import '../../../../features/clients/presentation/screens/trainer_clients_tab.dart';

/// Clients tab - Displays trainer's managed clients
class ClientsTab extends StatelessWidget {
  const ClientsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const TrainerClientsTab();
  }
}
